What is a Provider?
A Provider is a plugin that Terraform uses to interact with cloud platforms, SaaS providers, and other APIs. Providers are responsible for understanding API interactions and exposing resources.
Key Concepts
•	Providers are distributed separately from Terraform itself
•	Each provider adds a set of resource types and/or data sources
•	Providers are downloaded during terraform init
•	Multiple instances of the same provider can be configured using aliases
Best Practices
Always pin provider versions to avoid unexpected changes
Use environment variables for credentials instead of hardcoding
Use provider aliases for multi-region deployments
Document provider requirements in your README
--------------------------------------------------
What are Input Variables?
Input variables serve as parameters for a Terraform module, allowing customization without altering the module's source code. They make your configurations reusable and flexible.
 Auto-loaded Variable Files
Terraform automatically loads:

terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
When a variable is marked as sensitive:

Its value won't be shown in CLI output
It won't appear in plan output
Still stored in state file (encrypt your state!)
Variable Validation
variable "instance_type" {
  description = "EC2 instance type"
  type        = string
  default     = "t2.micro"

  validation {
    condition     = contains(["t2.micro", "t2.small", "t2.medium"], var.instance_type)
    error_message = "Instance type must be t2.micro, t2.small, or t2.medium."
  }
}

Variable Precedence (Lowest to Highest)
Default values in variable declaration
Environment variables (TF_VAR_*)
terraform.tfvars file
terraform.tfvars.json file
*.auto.tfvars or *.auto.tfvars.json files
-var and -var-file command line options
---------------------------------------
Terraform Output Variables
What are Output Variables?
Output variables expose information about your infrastructure on the command line and 
can share data between Terraform modules. 
They are useful for:
Displaying important resource attributes after apply
Passing data between modules
Providing information for external scripts or automation
Best Practices
Always add descriptions to outputs for documentation
Mark sensitive outputs appropriately
Use meaningful names that describe the value
Group related outputs together
Use outputs for module interfaces to expose necessary data
---------------------------------------------------
Terraform Resource Attributes
What are Resource Attributes?
Resource attributes are the properties of infrastructure resources that Terraform manages. There are two types:

Arguments: Values you set to configure the resource ( like a instance id creation)
Attributes: Values that are computed/returned by the provider after resource creation
( getting auto created like public ip ,You reference them, not set them)

Best Practices
Use attributes for dependencies instead of hardcoding values
Reference computed attributes in outputs for visibility
Use splat expressions for resources with count
Use for expressions for resources with for_each
Check documentation for all available attributes
------------------------
What are Resource Dependencies?
Resource dependencies define the order in which Terraform creates, updates, or destroys resources. Terraform uses dependencies to build a dependency graph and determine the correct order of operations.

Types of Dependencies
Implicit Dependencies: Automatically created when you reference resource attributes
  vpc_id     = aws_vpc.main.id    # <-- This creates an implicit dependenc
Explicit Dependencies: Manually specified using depends_on
Use depends_on when there's a dependency that Terraform can't automatically detect.
